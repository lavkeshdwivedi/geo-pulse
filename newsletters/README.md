# Newsletter Archive

This directory contains past GeoPulse newsletter editions.
Files are named `YYYY-MM-DD-HH.md` (UTC timestamp of generation).
Human-facing timestamps inside each newsletter use the configured local display timezone.
Old editions beyond `archive_days` (see `config.yml`) are pruned automatically.
